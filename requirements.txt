# Core data + numerics
numpy==2.1.3
pandas==2.2.3
scipy==1.15.3

# Market data — pinned: yfinance wraps an unofficial Yahoo Finance API and
# breaks frequently when Yahoo changes its endpoints or yfinance changes its
# column structure. 0.2.54 is the tested-and-working version.
yfinance==0.2.54

# Econometrics
arch==8.0.0
statsmodels==0.14.4

# Dashboard + plots
streamlit==1.45.1
plotly==6.1.1
