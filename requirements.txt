# Core data + numerics
numpy>=2.0
pandas>=2.2
scipy>=1.13

# Market data
yfinance>=0.2.50

# Econometrics
arch>=7.0          # univariate GARCH (Engle's two-step, step 1)
statsmodels>=0.14  # diagnostic tests (Jarque-Bera, ARCH-LM, Ljung-Box)

# Dashboard + plots
streamlit>=1.40
plotly>=5.20
